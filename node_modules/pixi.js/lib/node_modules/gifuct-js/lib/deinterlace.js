'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

require('../../../_virtual/_commonjsHelpers.js');
var deinterlace$1 = require('../../../_virtual/deinterlace2.js');

"use strict";
Object.defineProperty(deinterlace$1.__exports, "__esModule", {
  value: true
});
exports.deinterlace = deinterlace$1.__exports.deinterlace = void 0;
var deinterlace = function deinterlace2(pixels, width) {
  var newPixels = new Array(pixels.length);
  var rows = pixels.length / width;
  var cpRow = function cpRow2(toRow2, fromRow2) {
    var fromPixels = pixels.slice(fromRow2 * width, (fromRow2 + 1) * width);
    newPixels.splice.apply(newPixels, [toRow2 * width, width].concat(fromPixels));
  };
  var offsets = [0, 4, 2, 1];
  var steps = [8, 8, 4, 2];
  var fromRow = 0;
  for (var pass = 0; pass < 4; pass++) {
    for (var toRow = offsets[pass]; toRow < rows; toRow += steps[pass]) {
      cpRow(toRow, fromRow);
      fromRow++;
    }
  }
  return newPixels;
};
exports.deinterlace = deinterlace$1.__exports.deinterlace = deinterlace;

exports.default = deinterlace$1.__exports;
//# sourceMappingURL=deinterlace.js.map
